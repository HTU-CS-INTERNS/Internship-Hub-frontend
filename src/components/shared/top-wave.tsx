// This component is no longer primarily used for the main onboarding flow
// as the new design incorporates the wave into a larger bottom section.
// It's kept to avoid breaking changes if referenced elsewhere unexpectedly,
// or could be repurposed for a simple header wave if needed in other contexts.
import React from 'react';

const TopWave = () => {
  return null; 
};

export default TopWave;
