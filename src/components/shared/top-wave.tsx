// This component is no longer used by the primary onboarding flow.
// It's kept to avoid breaking changes if referenced elsewhere unexpectedly.
// For the new onboarding design, SVGs are directly embedded in page components.
import React from 'react';

const TopWave = () => {
  return null; // Or a minimal placeholder if required
};

export default TopWave;
